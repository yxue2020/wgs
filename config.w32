// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("wgs", "for wgs support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("wgs", "enable wgs support", "no");

if (PHP_WGS != "no") {
	EXTENSION("wgs", "wgs.c");
}

